import { YearsToNowPipe } from './years-to-now.pipe';

describe('YearsToNowPipe', () => {
  it('create an instance', () => {
    const pipe = new YearsToNowPipe();
    expect(pipe).toBeTruthy();
  });
});
